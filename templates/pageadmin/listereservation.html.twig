{% extends 'base.html.twig' %}

{% block title %}Page administrateur
{% endblock %}

{% block body %}

	<div class="container">

		<div class="row">
			{% for reservation in reservations %}


				<div class="col-lg-4">
					<div class="card">

						<div class="card-body">
                        {# regle par Vw#}
                            <p>{{ reservation.dateReservation|date('d/m/Y ,H') }} h </p>
							<p>{{reservation.nom}} {{reservation.prenom}}</p>
							<br>
							<p>{{ reservation.motif}}</p>
							<br>
							
						</div>


					</div>
				</div>

			{% endfor %}

		</div>
	</div>
{% endblock %}
