{% extends 'base.html.twig' %}

{% block title %}Modifier une réalisation{% endblock %}

{% block body %}

    <section class="container">

    {{form_start(form)}}
        {{form_widget(form)}}

            <a href="{{ path('supprimerRealisation', { id:realisation.id }) }}" class="btn btn-outline-danger">Supprimer</a>
            <button type="submit" class="btn btn-primary">Valider</button>    

    {{form_end(form)}}

    </section>

{% endblock %}
